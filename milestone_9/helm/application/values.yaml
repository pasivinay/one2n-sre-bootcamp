replicaCount: 1
image:
  repository: pasivinay/student-api
  tag: "1.0.1"
  pullPolicy: Always
database:
  secretName: db-secrets
service:
  type: NodePort
  port: 5000
deployment:
  nodeSelector:
    type: application